import 'package:flutter/material.dart';

class ConversarPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return new Scaffold(
      appBar: new AppBar(
        title: const Text('Conversar'),
      ),
      body: const Center(
        child: const Text('Under development'),
      ),
    );
  }
}